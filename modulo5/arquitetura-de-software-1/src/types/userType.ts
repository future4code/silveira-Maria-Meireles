export type usertype = {
    nome: string,
    email: string,
    password: string,
    role: string
}

export type user = {
    id: string,
    nome: string,
    email: string,
    password: string,
    role: string
}