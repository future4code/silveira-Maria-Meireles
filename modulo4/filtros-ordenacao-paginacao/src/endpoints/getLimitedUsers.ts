import express, { Request, Response } from 'express'
import { connection } from '../data/connection'

export const getLimitedUsers = async(
    req: Request,
    res: Response
): Promise<void> => {

}

const definesLimit = async():Promise<any> => {

}